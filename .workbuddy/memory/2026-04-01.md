# 2026-04-01

- 全仓扫描 LuxIso 当前工作树，确认 MovementComponent 的 pathTo/followPath/remainingWaypoints 已接入，index.html 的 minimap 与 hud-path DOM 已存在；构建 `npm run build:lib` 通过，但 `npm test` 在 `src/__tests__/Character.test.ts` 有 4 个失败。核心缺口收敛为：测试与新速度语义不同步、编辑器 props 与 Engine/Scene 序列化链路未闭合、Scene 缺少公开批量查询 API、缺少 resize 辅助。
- 修复 demo 的 `playerMv is not defined` 运行时错误：`src/main.ts` 改为直接使用 `Character` 自带的 `stopMoving/pathTo/isMoving/remainingWaypoints`，并移除多余的手动 `playerMv.update(ts)`；`npm run build` 已通过。

